// Promo title 1
(function () {
    var titleMainOne = document.querySelector('.promo__wrap__title__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('h1-title-1');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Promo title 2
(function () {
    var titleMainOne = document.querySelector('.promo__wrap__subtitle__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('h1-title-2');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Promo offer
(function () {
    var titleMainOne = document.querySelector('.promo__wrap__offer');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('offer');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Promo btn
(function () {
    var titleMainOne = document.querySelector('.promo__wrap__btn');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('btn-promo');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// About title
(function () {
    var titleMainOne = document.querySelector('.about__title-mob');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-title-mob');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// About title
(function () {
    var titleMainOne = document.querySelector('.about__wrap__content__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-title-mob');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// About item
(function () {
    var titleMainOne = document.querySelector('.about-item-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// About item
(function () {
    var titleMainOne = document.querySelector('.about-item-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// About item
(function () {
    var titleMainOne = document.querySelector('.about-item-3');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// About item
(function () {
    var titleMainOne = document.querySelector('.about-item-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// About item
(function () {
    var titleMainOne = document.querySelector('.about-item-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// About photo
(function () {
    var titleMainOne = document.querySelector('.about__wrap__images__main');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-photo');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// About Photo 2
(function () {
    var titleMainOne = document.querySelector('.about__wrap__images__two');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-photo-2');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Excursion title
(function () {
    var titleMainOne = document.querySelector('.excursion__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-title-mob');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-3');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-7');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-8');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-9');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-10');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-11');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-12');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-13');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-14');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-15');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Excursion item
(function () {
    var titleMainOne = document.querySelector('.exc-16');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();



// Excursion item




// Excursion item






// Advantages title
(function () {
    var titleMainOne = document.querySelector('.advantages__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-title-mob');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Advantages item
(function () {
    var titleMainOne = document.querySelector('.adv-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Advantages item
(function () {
    var titleMainOne = document.querySelector('.adv-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Advantages item
(function () {
  var titleMainOne = document.querySelector('.adv-3');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        entry.target.classList.add('exc-item');
      }
    });
  });

  observer.observe(titleMainOne);
})
();


// Advantages item
(function () {
  var titleMainOne = document.querySelector('.adv-4');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        entry.target.classList.add('exc-item');
      }
    });
  });

  observer.observe(titleMainOne);
})
();


// Advantages item
(function () {
    var titleMainOne = document.querySelector('.adv-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Advantages item
(function () {
    var titleMainOne = document.querySelector('.adv-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Reviews title
(function () {
    var titleMainOne = document.querySelector('.reviews__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-title-mob');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Reviews photo
(function () {
    var titleMainOne = document.querySelector('.reviews__wrap');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Reviews btn
(function () {
    var titleMainOne = document.querySelector('.prev-slider1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Reviews btn
(function () {
    var titleMainOne = document.querySelector('.next-slider1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page title 
(function () {
    var titleMainOne = document.querySelector('.intro__wrap__content__title-mob__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('page-title');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();
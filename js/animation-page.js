// Page title 
(function () {
    var pageNameOne = document.querySelector('.intro__wrap__content__title-mob__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('page-title-1');
        }
      });
    });
  
    observer.observe(pageNameOne);
  })
();




// Page title 
(function () {
    var pageNameIntro = document.querySelector('.intro__wrap__content__subtitle-mob__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('page-title-2');
        }
      });
    });
  
    observer.observe(pageNameIntro);
  })
();




// Page title 
(function () {
  var pageNameGe = document.querySelector('.intro__wrap__content__title__text');

  var observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
        return;
      }

      if (entry.isIntersecting) {
        entry.target.classList.add('page-title-3');
      }
    });
  });

  observer.observe(pageNameGe);
})
();







// Page title 
(function () {
    var pageNameFour = document.querySelector('.intro__wrap__content__subtitle__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('page-title-2');
        }
      });
    });
  
    observer.observe(pageNameFour);
  })
();






// Page slider
(function () {
    var titleMainOne = document.querySelector('.intro__wrap__gallery');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-3');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page description item
(function () {
    var titleMainOne = document.querySelector('.descrp-item-7');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();






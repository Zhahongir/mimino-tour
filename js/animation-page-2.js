// Page time
(function () {
    var titleMainOne = document.querySelector('.intro__wrap__content__time');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item-2');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page money
(function () {
    var titleMainOne = document.querySelector('.intro__wrap__content__price');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('opacity-item-2');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page btn
(function () {
    var titleMainOne = document.querySelector('.intro__wrap__content__btn');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.includes__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-3');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-7');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();




// Page include title
(function () {
    var titleMainOne = document.querySelector('.inkl-8');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('exc-item');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();







